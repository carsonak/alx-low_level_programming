#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char thisStrng[] = "\"Programming is like building a multilingual puzzle";

	puts(thisStrng);

	return (0);
}

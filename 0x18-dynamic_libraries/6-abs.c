#include "main.h"

/**
 * _abs - return the absolute value of a number.
 * @n: signed number to convert.
 *
 * Return: absolute value of n.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);

	return (-n);
}
